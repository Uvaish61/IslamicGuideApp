import React from 'react';
import { View, Text } from 'react-native';

const BottomNav = () => {
  // scaffold: will render bottom navigation tabs
  return (
    <View style={{ height: 60, backgroundColor: 'transparent' }} />
  );
};

export default BottomNav;
